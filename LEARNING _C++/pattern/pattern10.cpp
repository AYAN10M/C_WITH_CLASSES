#include <bits/stdc++.h>
using namespace std;

int main()
{
    int row;
    cout << "Enter the no of row:\t";
    cin >> row;
    for (int i = 0; i < row; i++)
    {
        for (int j = 0; i < count; i++)
        {
            /* code */
        }
        
    }
    
    return 0;
}